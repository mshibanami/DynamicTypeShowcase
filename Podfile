# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DynamicFontShowcase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DynamicFontShowcase
  pod 'GrowingTextView', git: 'https://github.com/KennethTsang/GrowingTextView.git', branch: 'swift4'

  target 'DynamicFontShowcaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DynamicFontShowcaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end
end
